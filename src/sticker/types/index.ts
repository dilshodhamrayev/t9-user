export enum StickerTypes {
    STICKER = 1,
    COUPON = 2
}

export enum StickerStatuses {
    WAITING_OWNDER = 15,
    WAITING_REGISTER = 17,
    REGISTERED = 11,
    EXPIRED = 31,
    WINNED = 26,
    ARCHIVED = 52
}